VITE_GAME_SERVER_URL=ws://localhost:2567

# Optional: wallet login (see WalletPanel). Leave unset to disable it —
# guests can still join and play normally. Must match the game-server's
# PRIVY_APP_ID (see apps/game-server/.env.example).
VITE_PRIVY_APP_ID=
